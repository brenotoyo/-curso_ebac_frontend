function multplicacao (valor1: number, valor2: number): number {
    return valor1 * valor2;
}

function saudar(nome: string): string {
    return `Olá ${nome}`;
}

console.log(multplicacao(2, 3));
console.log(saudar("Breno"));